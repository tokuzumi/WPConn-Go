version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playground-ia-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production